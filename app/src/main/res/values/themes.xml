<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Relative_Layout" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/placeholder</item>
        <item name="colorControlActivated">@color/primary</item>
    </style>


    <style name="Theme.Relative_Layout" parent="Base.Theme.Relative_Layout" />

    <style name="MyTransparentBottomSheetDialogTheme" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/transparent</item>
        <item name="android:colorBackground">@color/transparent</item>
    </style>

    <style name="CustomTabText" parent="CustomTabText" tools:ignore="ResourceCycle">
        <item name="android:textSize">20sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="fontFamily">@font/red_hat_font_family</item>
    </style>
</resources>